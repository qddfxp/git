# FreeCAD建模任务：带中心凸台的倒角圆柱

做一个圆柱，顶面中心加个凸台，底面外圆倒个角。只建模存盘，不要自行验收、不要核对尺寸、不要生成验收报告，存完即完成。

## 环境

FreeCAD 1.1.3 装在 E:\freecad\bin（freecadcmd.exe 和 freecad.exe 同目录）；用户宏目录已配为 E:\jiaobenmulu，脚本存该目录；模型存桌面 freecad 文件夹。

## 执行

脚本和 freecadcmd 无头调试先行，不用等 GUI。GUI 里先 Ctrl+N 新建文档，再经"宏 (M)→宏…→选中脚本→执行"跑一次。别走 Python 控制台粘贴。

脚本自建文档、存盘，末尾 Body.ViewObject 设可见 + FitAll 一次。同名 FCStd 直接覆盖。

启动前先结束所有残留 FreeCAD 进程。E:\jiaobenmulu 写入被拒就排查一次，还不行暂存桌面 freecad 文件夹并注明。

## 参数

草图基准 XY 平面，+Z 拉伸，底面 Z=0。

- 主体圆柱：直径 50 mm，高 30 mm，圆心对原点
- 顶面中心凸台：直径 25 mm，高 10 mm，与主体同轴（过原点 Z 轴），凸台底面与主体顶面重合
- 底面外圆倒角 C3 mm——仅底面与侧面相交的外圆环棱，顶面棱、凸台棱、孔口棱不倒

## 草图约束

约束方式不限，允许按精确坐标摆几何再用 Block 整体锁位。要求全约束、单闭合线框。不强制构造圆加等长。

## 建模要求

全用 Part Design 原生特征，同一个 Body，自动合成单一实体。禁 Part 模块直生几何体，禁布尔拼接。

## 理论参考（仅定义正确性，不需要执行验收）

以下数值用于定义"做对"的标准，建模时不需要核对、不需要跑验收脚本、不需要输出验收结论：
- 单一 Body、单一实体
- 特征 TypeId 白名单：Sketcher::SketchObject、PartDesign::Pad、PartDesign::Chamfer；禁 Part 造体和布尔
- z=15 水平截面为直径 50 的圆（Part.Face(wire).Area 校验）
- 凸台半径 12.5，Z∈[30,40]，与主体同轴
- 底面外圆 C3 倒角面恰好 1 张（锥形环面）
- 理论体积 ≈63135 mm³，容差 1%

## 调试卡点

建模脚本零求解器告警，告警即换约束方案，同一方案最多试两次。

一个卡点最多想两个办法，第一个试两次没进展就换第二个，别重复失败路径。脚本报错先查根因，改完整体重跑，不逐行打补丁。

## 交付

只要 `cylinder_with_boss_chamfer.FCStd`，存桌面 freecad 文件夹。脚本、验收报告之类不用交。

## 验收核对清单（Checklist）

```plaintext
1. cylinder_with_boss_chamfer.FCStd 位于桌面 freecad 文件夹，文件可正常打开无损坏
2. 模型为单一 Body 下的单一实体，特征 TypeId 均在白名单内，无 Part 造体或布尔运算
3. 主体圆柱直径 50 mm、高 30 mm，圆心对原点，XY 草图、+Z 拉伸、底面 Z=0
4. 顶面中心凸台直径 25 mm、高 10 mm，与主体同轴，凸台底面与主体顶面重合
5. 底面外圆倒角 C3 mm，其余棱未倒角
6. 模型体积 ≈63135 mm³，偏差在 1% 以内
```
