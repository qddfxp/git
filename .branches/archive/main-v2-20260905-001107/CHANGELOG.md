# 主线版本记录

## v1 — 2026-09-04 21:53:15
- 初始化主线，固化长期规则。

## v2 — 2026-09-04 22:25:49
- 来源分支：checklist-concise（基于 v1）
- 提升说明：Checklist从单条单事实(15-25条)改为按维度合并5-6条，用户确认效果满意
- 规则差异：

```diff
--- v1/PROMPT.md
+++ v2/PROMPT.md
@@ -1,8 +1,7 @@
-# 主线长期规则（main/PROMPT.md）
+# 分支候选规则（branches/checklist-concise/PROMPT.md）
 
-> 本文件是这条长期任务对话的**唯一事实来源**：任务目标、长期提示词、产出规范、
-> 风格与格式要求都固化在这里。每次执行正式任务前重读本文件；聊天中的临时说法
-> 不构成主线规则。主线规则只能通过「开分支试跑 → 对比 → promote」修改，不直接手改。
+> 本文件是分支 checklist-concise 的候选规则，仅在本分支试跑期间生效。
+> 实验目的：Checklist 从逐条细拆压缩到 5-6 条，按维度合并同类事实，保留核心判据。
 
 ## 1. 任务定位
 
@@ -48,9 +47,9 @@
 ### 3.2 验收核对清单（Checklist）硬性规则
 
 1. **严格对应题目内容，禁止加题外项**：所有验收条目必须 100% 来自题目明确给出的要求、参数、约束，不得新增题目未提及的判定标准（如禁止出现 "无尖锐棱边残留" 等主观描述）。
-2. **单条单事实，可客观判定**：每条仅包含一个可独立验证的事实，附带明确数值 / 状态判据，无模糊、主观表述。
-3. **全覆盖无遗漏**：必须覆盖题目所有核心要求，包括：主体尺寸、核心特征尺寸、棱边处理参数、模型合规性（同一 Body、单一实体、Part Design 原生特征）、文件交付（命名、存储路径）、交付内容要求。
-4. **条目独立**：各验收项之间无重叠、无包含，每条可单独判定通过 / 不通过。
+2. **按维度合并，可客观判定**：每条聚焦一个验收维度（如文件交付、模型合规、主体尺寸、核心特征、棱边处理），可包含该维度下的多个数值/状态判据，整体可一次性判定通过/不通过，无模糊、主观表述。
+3. **条目总数控制在 5-6 条**：不得超过 6 条，也不得少于 5 条；通过按维度合并同类事实实现压缩，不得通过遗漏核心要求来凑数。
+4. **全覆盖无遗漏**：必须覆盖题目所有核心要求，包括：主体尺寸、核心特征尺寸、棱边处理参数、模型合规性（同一 Body、单一实体、Part Design 原生特征）、文件交付（命名、存储路径）、交付内容要求。
 5. **格式**：用 ```plaintext 代码块包裹，条目编号清晰。
 
 ## 4. 风格与质量标准
@@ -59,6 +58,7 @@
 - 建模难度控制为简单级，特征数量 2-3 个
 - 尺寸数值明确、定位关系清晰
 - Checklist 条目可客观判定，无主观模糊描述
+- Checklist 总数 5-6 条，精炼不冗余
 
 ## 5. 例外与边界
 
@@ -67,3 +67,4 @@
 - 不得出现主观模糊描述
 - 不得遗漏题目明确的交付与约束要求
 - 不得使用复杂结构模型，控制建模难度为简单级
+- Checklist 不得超过 6 条或少于 5 条

```
- 上一版主线完整归档：archive/main-v1-20260904-222549/（回滚命令：python cb.py rollback "E:\freecad题目创建" 1）
