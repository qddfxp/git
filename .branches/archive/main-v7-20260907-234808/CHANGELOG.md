# 主线版本记录

## v1 — 2026-09-04 21:53:15
- 初始化主线，固化长期规则。

## v2 — 2026-09-04 22:25:49
- 来源分支：checklist-concise（基于 v1）
- 提升说明：Checklist从单条单事实(15-25条)改为按维度合并5-6条，用户确认效果满意
- 规则差异：

```diff
--- v1/PROMPT.md
+++ v2/PROMPT.md
@@ -1,8 +1,7 @@
-# 主线长期规则（main/PROMPT.md）
+# 分支候选规则（branches/checklist-concise/PROMPT.md）
 
-> 本文件是这条长期任务对话的**唯一事实来源**：任务目标、长期提示词、产出规范、
-> 风格与格式要求都固化在这里。每次执行正式任务前重读本文件；聊天中的临时说法
-> 不构成主线规则。主线规则只能通过「开分支试跑 → 对比 → promote」修改，不直接手改。
+> 本文件是分支 checklist-concise 的候选规则，仅在本分支试跑期间生效。
+> 实验目的：Checklist 从逐条细拆压缩到 5-6 条，按维度合并同类事实，保留核心判据。
 
 ## 1. 任务定位
 
@@ -48,9 +47,9 @@
 ### 3.2 验收核对清单（Checklist）硬性规则
 
 1. **严格对应题目内容，禁止加题外项**：所有验收条目必须 100% 来自题目明确给出的要求、参数、约束，不得新增题目未提及的判定标准（如禁止出现 "无尖锐棱边残留" 等主观描述）。
-2. **单条单事实，可客观判定**：每条仅包含一个可独立验证的事实，附带明确数值 / 状态判据，无模糊、主观表述。
-3. **全覆盖无遗漏**：必须覆盖题目所有核心要求，包括：主体尺寸、核心特征尺寸、棱边处理参数、模型合规性（同一 Body、单一实体、Part Design 原生特征）、文件交付（命名、存储路径）、交付内容要求。
-4. **条目独立**：各验收项之间无重叠、无包含，每条可单独判定通过 / 不通过。
+2. **按维度合并，可客观判定**：每条聚焦一个验收维度（如文件交付、模型合规、主体尺寸、核心特征、棱边处理），可包含该维度下的多个数值/状态判据，整体可一次性判定通过/不通过，无模糊、主观表述。
+3. **条目总数控制在 5-6 条**：不得超过 6 条，也不得少于 5 条；通过按维度合并同类事实实现压缩，不得通过遗漏核心要求来凑数。
+4. **全覆盖无遗漏**：必须覆盖题目所有核心要求，包括：主体尺寸、核心特征尺寸、棱边处理参数、模型合规性（同一 Body、单一实体、Part Design 原生特征）、文件交付（命名、存储路径）、交付内容要求。
 5. **格式**：用 ```plaintext 代码块包裹，条目编号清晰。
 
 ## 4. 风格与质量标准
@@ -59,6 +58,7 @@
 - 建模难度控制为简单级，特征数量 2-3 个
 - 尺寸数值明确、定位关系清晰
 - Checklist 条目可客观判定，无主观模糊描述
+- Checklist 总数 5-6 条，精炼不冗余
 
 ## 5. 例外与边界
 
@@ -67,3 +67,4 @@
 - 不得出现主观模糊描述
 - 不得遗漏题目明确的交付与约束要求
 - 不得使用复杂结构模型，控制建模难度为简单级
+- Checklist 不得超过 6 条或少于 5 条

```
- 上一版主线完整归档：archive/main-v1-20260904-222549/（回滚命令：python cb.py rollback "E:\freecad题目创建" 1）

## v3 — 2026-09-05 00:11:07
- 来源分支：natural-style（基于 v2）
- 提升说明：去AI味+反模板：信息必达但组织自由，语言自然多变，用户确认效果满意
- 规则差异：

```diff
--- v2/PROMPT.md
+++ v3/PROMPT.md
@@ -1,7 +1,7 @@
-# 分支候选规则（branches/checklist-concise/PROMPT.md）
+# 分支候选规则（branches/natural-style/PROMPT.md）
 
-> 本文件是分支 checklist-concise 的候选规则，仅在本分支试跑期间生效。
-> 实验目的：Checklist 从逐条细拆压缩到 5-6 条，按维度合并同类事实，保留核心判据。
+> 本文件是分支 natural-style 的候选规则，仅在本分支试跑期间生效。
+> 实验目的：去除AI味，打破固定模板，语言自然多变但核心要求不丢。
 
 ## 1. 任务定位
 
@@ -16,38 +16,39 @@
 
 ## 3. 长期提示词 / 工作规则
 
-### 3.1 题目固定结构与内容要求
+### 3.1 题目内容要求（信息必达，表述自由）
 
-#### 标题与开篇
-- 标题格式：`FreeCAD建模任务：XXX`
-- 开篇说明：`请使用 FreeCAD 完成 XX 的参数化建模，模型生成后直接保存文件即可，无需额外开展尺寸核对、特征校验类操作，精简执行流程。`
+以下信息点必须出现在题目中，但**组织方式、措辞、段落顺序均可灵活变化**，禁止每题套用同一模板：
 
-#### 执行要求（固定模块，可按需微调表述）
-必须包含以下 4 项，表述统一：
-1. **前置准备**：全新启动 FreeCAD 1.1.3 客户端，处理弹窗至主界面稳定运行，切换至 Part Design 工作台；检查桌面 freecad 存储文件夹，不存在则新建。准备就绪后输出 "GUI 环境初始化完成"，在此之前不得编写任何建模脚本或创建代码文件。
-2. **脚本规范**：建模所用 Python 脚本统一存放至 `E:\jiaobenmulu` 目录，禁止写入软件安装路径或系统目录；API 属性调用以 1.1.3 版本为准。
-3. **效率提示**：支持无头预验证模式，可先通过 freecadcmd 完成脚本逻辑调试，GUI 仅执行一次最终脚本即可，无需界面反复调试。
-4. **模型约束**：最终实体必须为 Part Design 工作台下的原生参数化特征，全部特征归属同一 Body 并自动融合为单一实体；禁止使用 Part 模块直接生成几何体，也不允许通过布尔运算拼接模型。
+#### 必含信息点
+- **标题**：含"FreeCAD建模任务"和模型名称，格式不必每次相同
+- **任务说明**：用一两句话说清要建什么、建模后直接保存即可
+- **环境准备**：全新启动 FreeCAD 1.1.3 → Part Design 工作台 → 检查桌面 freecad 文件夹 → 输出"GUI 环境初始化完成"后才能写脚本
+- **脚本存放**：统一放 `E:\jiaobenmulu`，API 以 1.1.3 为准
+- **调试建议**：可用 freecadcmd 无头预验证，GUI 只跑最终脚本
+- **模型约束**：Part Design 原生特征、同一 Body、单一实体、禁止 Part 模块直生几何体、禁止布尔拼接
+- **尺寸参数**：XY 平面基准、+Z 拉伸、Z=0 底面；主体尺寸 + 核心特征尺寸 + 棱边处理（倒角/圆角）；特征 2-3 个；数值明确、定位清晰
+- **操作准则**：单卡点最多两种思路，第一种两次失败即切换；报错先找根因再整体重跑
+- **交付标准**：英文文件名.FCStd → 桌面 freecad 文件夹 → 交付绝对路径 + 模型树特征顺序
 
-#### 尺寸参数
-- 模型选择简单易建模的基础几何体，特征数量控制在 2-3 个，避免复杂结构
-- 明确基准：默认以 XY 平面为草图基准，+Z 为拉伸方向，Z=0 平面为模型底面
-- 所有尺寸标注明确数值，定位关系清晰（如 "中心同轴""居中对齐"），无模糊描述
-- 包含：主体尺寸 + 核心特征尺寸 + 棱边处理（倒角 / 圆角，二选一或组合均可）
+#### 表述自由度
+- 不必每次都用"执行要求""尺寸参数""操作准则""交付标准"这几个完全相同的二级标题
+- 可以把环境准备和脚本规范合并成一段"开始之前"，也可以把调试建议融入模型约束
+- 开篇说明可以换说法，不必每次都是"请使用 FreeCAD 完成 XX 的参数化建模，模型生成后直接保存文件即可…"
+- 核心是信息到位、读起来像人写的，而不是填表
 
-#### 操作准则（固定规则）
-单个卡点最多构思两种解决思路，第一种方案尝试两次仍无法推进则立刻切换第二种，不重复已验证失败的路径；脚本报错先定位根本原因，修正后整体重跑，不逐行补丁式调试。
+### 3.2 语言风格（去AI味）
 
-#### 交付标准
-必须包含 3 项：
-1. **文件命名**：英文文件名.FCStd
-2. **存储位置**：桌面 freecad 文件夹
-3. **交付内容**：文件完整绝对路径 + 模型树特征排列顺序
+- **说人话**：用自然口语化的书面表达，避免"综上所述""值得注意的是""需要指出的是"这类AI套话
+- **不堆排比**：不要每条都用相同句式开头（如连续的"必须……""应当……""确保……"）
+- **不写正确的废话**：删掉"请仔细阅读以下要求""按照步骤逐步完成"这类无信息量的句子
+- **长短句交错**：避免全是结构工整的长句，适当用短句、断句
+- **每题换说法**：同一信息点在不同题目中用不同措辞表达，不复制粘贴
 
-### 3.2 验收核对清单（Checklist）硬性规则
+### 3.3 验收核对清单（Checklist）硬性规则
 
-1. **严格对应题目内容，禁止加题外项**：所有验收条目必须 100% 来自题目明确给出的要求、参数、约束，不得新增题目未提及的判定标准（如禁止出现 "无尖锐棱边残留" 等主观描述）。
-2. **按维度合并，可客观判定**：每条聚焦一个验收维度（如文件交付、模型合规、主体尺寸、核心特征、棱边处理），可包含该维度下的多个数值/状态判据，整体可一次性判定通过/不通过，无模糊、主观表述。
+1. **严格对应题目内容，禁止加题外项**：所有验收条目必须 100% 来自题目明确给出的要求、参数、约束，不得新增题目未提及的判定标准。
+2. **按维度合并，可客观判定**：每条聚焦一个验收维度，可包含该维度下的多个数值/状态判据，整体可一次性判定通过/不通过，无模糊、主观表述。
 3. **条目总数控制在 5-6 条**：不得超过 6 条，也不得少于 5 条；通过按维度合并同类事实实现压缩，不得通过遗漏核心要求来凑数。
 4. **全覆盖无遗漏**：必须覆盖题目所有核心要求，包括：主体尺寸、核心特征尺寸、棱边处理参数、模型合规性（同一 Body、单一实体、Part Design 原生特征）、文件交付（命名、存储路径）、交付内容要求。
 5. **格式**：用 ```plaintext 代码块包裹，条目编号清晰。
@@ -57,8 +58,9 @@
 - 简洁、考点明确，避免非考点冗余内容
 - 建模难度控制为简单级，特征数量 2-3 个
 - 尺寸数值明确、定位关系清晰
-- Checklist 条目可客观判定，无主观模糊描述
-- Checklist 总数 5-6 条，精炼不冗余
+- Checklist 条目可客观判定，无主观模糊描述，总数 5-6 条
+- 语言自然，无AI套话和模板感
+- 每题结构和措辞有变化，不重复同一模板
 
 ## 5. 例外与边界
 
@@ -68,3 +70,5 @@
 - 不得遗漏题目明确的交付与约束要求
 - 不得使用复杂结构模型，控制建模难度为简单级
 - Checklist 不得超过 6 条或少于 5 条
+- 不得使用AI套话、排比堆砌、正确的废话
+- 不得每题套用完全相同的标题结构和措辞

```
- 上一版主线完整归档：archive/main-v2-20260905-001107/（回滚命令：python cb.py rollback "E:\freecad题目创建" 2）

## v4 — 2026-09-06 21:24:46
- 来源分支：checklist-wording（基于 v3）
- 提升说明：Checklist话术多变+题目间低一致性：禁止固定开头词和句式，题目间模型/特征/尺寸/结构/措辞均不重复
- 规则差异：

```diff
--- v3/PROMPT.md
+++ v4/PROMPT.md
@@ -1,7 +1,7 @@
-# 分支候选规则（branches/natural-style/PROMPT.md）
+# 分支候选规则（branches/checklist-wording/PROMPT.md）
 
-> 本文件是分支 natural-style 的候选规则，仅在本分支试跑期间生效。
-> 实验目的：去除AI味，打破固定模板，语言自然多变但核心要求不丢。
+> 本文件是分支 checklist-wording 的候选规则，仅在本分支试跑期间生效。
+> 实验目的：Checklist 话术多变 + 题目间低一致性，避免固定模板和重复套路。
 
 ## 1. 任务定位
 
@@ -33,8 +33,8 @@
 
 #### 表述自由度
 - 不必每次都用"执行要求""尺寸参数""操作准则""交付标准"这几个完全相同的二级标题
-- 可以把环境准备和脚本规范合并成一段"开始之前"，也可以把调试建议融入模型约束
-- 开篇说明可以换说法，不必每次都是"请使用 FreeCAD 完成 XX 的参数化建模，模型生成后直接保存文件即可…"
+- 可以把环境准备和脚本规范合并成一段，也可以把调试建议融入模型约束
+- 开篇说明可以换说法，不必每次都是同一句
 - 核心是信息到位、读起来像人写的，而不是填表
 
 ### 3.2 语言风格（去AI味）
@@ -45,13 +45,26 @@
 - **长短句交错**：避免全是结构工整的长句，适当用短句、断句
 - **每题换说法**：同一信息点在不同题目中用不同措辞表达，不复制粘贴
 
-### 3.3 验收核对清单（Checklist）硬性规则
+### 3.3 题目间低一致性（硬性要求）
+
+每道新题必须与已产出的所有题目保持**低一致性**，不得换个数字就复用同一套模型和结构。具体要求：
+
+- **模型类型不重复**：主体几何体类型要有明显差异（如长方体、圆柱、六棱柱、圆台、圆盘、楔形、梯形台等交替使用），不得连续两题用同类型主体
+- **特征组合不重复**：核心特征类型要换（凸台/通孔/盲孔/凹槽/沉孔/偏心孔/切槽等交替），棱边处理也要换（圆角/倒角交替，或处理位置不同）
+- **尺寸量级有差异**：各题主体尺寸不要都在同一量级（如不要连续几题都是 50-80mm 见方），适当拉开大小差异
+- **结构组织不重复**：二级标题名称、段落顺序、信息合并方式每题不同，不得连续两题用同一套标题
+- **开篇和措辞不重复**：任务说明的句式、用词每题换，不得出现"这次建一个…""做一个…"连续重复
+- **Checklist 话术不重复**：各题 Checklist 的开头词、句式、表达角度全部不同，不得复用上一题的表述方式
+- **自检**：出完新题后，与最近 3 道题对比，确认模型类型、特征组合、标题结构、开篇句式、Checklist 话术至少有 4 项不同
+
+### 3.4 验收核对清单（Checklist）硬性规则
 
 1. **严格对应题目内容，禁止加题外项**：所有验收条目必须 100% 来自题目明确给出的要求、参数、约束，不得新增题目未提及的判定标准。
 2. **按维度合并，可客观判定**：每条聚焦一个验收维度，可包含该维度下的多个数值/状态判据，整体可一次性判定通过/不通过，无模糊、主观表述。
 3. **条目总数控制在 5-6 条**：不得超过 6 条，也不得少于 5 条；通过按维度合并同类事实实现压缩，不得通过遗漏核心要求来凑数。
 4. **全覆盖无遗漏**：必须覆盖题目所有核心要求，包括：主体尺寸、核心特征尺寸、棱边处理参数、模型合规性（同一 Body、单一实体、Part Design 原生特征）、文件交付（命名、存储路径）、交付内容要求。
-5. **格式**：用 ```plaintext 代码块包裹，条目编号清晰。
+5. **话术多变，禁止固定开头**：每条必须使用不同的句式结构和开头词，同一题目内各条开头词不得重复。禁止套路化表达连续出现。可交替使用"检查…是否…""确认…为…""…应满足…""…符合…""…达到…""…处于…""核实…""验证…"等多种表达。不同题目之间 Checklist 话术也要换，不复制上一题的句式。
+6. **格式**：用 ```plaintext 代码块包裹，条目编号清晰。
 
 ## 4. 风格与质量标准
 
@@ -59,8 +72,10 @@
 - 建模难度控制为简单级，特征数量 2-3 个
 - 尺寸数值明确、定位关系清晰
 - Checklist 条目可客观判定，无主观模糊描述，总数 5-6 条
+- Checklist 每条话术不同，开头词不重复，无固定套路
 - 语言自然，无AI套话和模板感
 - 每题结构和措辞有变化，不重复同一模板
+- 题目间低一致性：模型类型、特征组合、尺寸量级、标题结构、开篇句式、Checklist 话术均不重复
 
 ## 5. 例外与边界
 
@@ -72,3 +87,7 @@
 - Checklist 不得超过 6 条或少于 5 条
 - 不得使用AI套话、排比堆砌、正确的废话
 - 不得每题套用完全相同的标题结构和措辞
+- Checklist 各条不得使用相同开头词或固定句式套路
+- 不得连续两题使用同类型主体几何体
+- 不得连续两题使用相同的核心特征类型
+- 不得换个数字就复用同一套模型结构和标题组织

```
- 上一版主线完整归档：archive/main-v3-20260906-212446/（回滚命令：python cb.py rollback "E:\freecad题目创建" 3）

## v5 — 2026-09-06 21:45:24
- 来源分支：task-style-v2（基于 v4）
- 提升说明：8条写法改进(GUI解耦/目录兜底/进程清理/多边形朝向/盲孔类型/棱边范围/独立验收/保留卡点)+完整Checklist规范(三层框架/8条硬规则/输出前自检维度表)
- 规则差异：

```diff
--- v4/PROMPT.md
+++ v5/PROMPT.md
@@ -1,7 +1,7 @@
-# 分支候选规则（branches/checklist-wording/PROMPT.md）
+# 分支候选规则（branches/task-style-v2/PROMPT.md）
 
-> 本文件是分支 checklist-wording 的候选规则，仅在本分支试跑期间生效。
-> 实验目的：Checklist 话术多变 + 题目间低一致性，避免固定模板和重复套路。
+> 本文件是分支 task-style-v2 的候选规则，仅在本分支试跑期间生效。
+> 实验目的：8条写法改进——GUI与脚本解耦、目录兜底、进程清理、多边形朝向、盲孔类型、棱边范围、独立验收脚本、保留卡点规则。
 
 ## 1. 任务定位
 
@@ -22,60 +22,86 @@
 
 #### 必含信息点
 - **标题**：含"FreeCAD建模任务"和模型名称，格式不必每次相同
-- **任务说明**：用一两句话说清要建什么、建模后直接保存即可
-- **环境准备**：全新启动 FreeCAD 1.1.3 → Part Design 工作台 → 检查桌面 freecad 文件夹 → 输出"GUI 环境初始化完成"后才能写脚本
-- **脚本存放**：统一放 `E:\jiaobenmulu`，API 以 1.1.3 为准
-- **调试建议**：可用 freecadcmd 无头预验证，GUI 只跑最终脚本
+- **任务说明**：用一两句话说清要建什么
+- **环境准备**：先检查并结束所有残留 FreeCAD 进程，再全新启动 FreeCAD 1.1.3 → 处理弹窗 → Part Design 工作台 → 检查桌面 freecad 文件夹（不存在则新建）
+- **脚本与调试解耦**：脚本编写和 freecadcmd 无头调试可先行，不必等 GUI；GUI 初始化后仅执行一次最终脚本，不在 GUI 里反复调试
+- **脚本存放与兜底**：脚本统一放 `E:\jiaobenmulu`；若写入被拒，排查一次仍不行则暂存桌面 freecad 文件夹并注明，不卡死
 - **模型约束**：Part Design 原生特征、同一 Body、单一实体、禁止 Part 模块直生几何体、禁止布尔拼接
 - **尺寸参数**：XY 平面基准、+Z 拉伸、Z=0 底面；主体尺寸 + 核心特征尺寸 + 棱边处理（倒角/圆角）；特征 2-3 个；数值明确、定位清晰
+- **多边形朝向**：使用正多边形时必须指定朝向（如"平边朝上""尖角朝右"），不得只给尺寸不标朝向
+- **孔类型明确**：盲孔必须写明"平底盲孔"或指定钻尖角（如 118°）；通孔、沉孔等也写明类型
+- **棱边范围封死**：倒角/圆角必须明确指定哪些棱（如"仅外轮廓六条棱，孔口棱不倒"），不得有歧义
+- **文件覆盖**：同名 FCStd 文件直接覆盖，不另存版本号
 - **操作准则**：单卡点最多两种思路，第一种两次失败即切换；报错先找根因再整体重跑
-- **交付标准**：英文文件名.FCStd → 桌面 freecad 文件夹 → 交付绝对路径 + 模型树特征顺序
+- **交付标准**：英文文件名.FCStd → 桌面 freecad 文件夹 → 交付绝对路径 + 模型树特征排列顺序
+- **独立验收脚本**：建模脚本只负责建模 + 存盘，不写合格判定；另需一个独立验收脚本，从磁盘重新打开 FCStd 回读，按 Checklist 逐项输出验收结论
 
 #### 表述自由度
-- 不必每次都用"执行要求""尺寸参数""操作准则""交付标准"这几个完全相同的二级标题
-- 可以把环境准备和脚本规范合并成一段，也可以把调试建议融入模型约束
-- 开篇说明可以换说法，不必每次都是同一句
+- 不必每次都用固定的二级标题，可自由组织
+- 可以把相关信息合并成一段，也可以拆开
+- 开篇说明可以换说法
 - 核心是信息到位、读起来像人写的，而不是填表
 
 ### 3.2 语言风格（去AI味）
 
-- **说人话**：用自然口语化的书面表达，避免"综上所述""值得注意的是""需要指出的是"这类AI套话
-- **不堆排比**：不要每条都用相同句式开头（如连续的"必须……""应当……""确保……"）
-- **不写正确的废话**：删掉"请仔细阅读以下要求""按照步骤逐步完成"这类无信息量的句子
-- **长短句交错**：避免全是结构工整的长句，适当用短句、断句
-- **每题换说法**：同一信息点在不同题目中用不同措辞表达，不复制粘贴
+- **说人话**：用自然口语化的书面表达，避免AI套话
+- **不堆排比**：不要每条都用相同句式开头
+- **不写正确的废话**：删掉无信息量的句子
+- **长短句交错**：避免全是结构工整的长句
+- **每题换说法**：同一信息点在不同题目中用不同措辞表达
 
 ### 3.3 题目间低一致性（硬性要求）
 
-每道新题必须与已产出的所有题目保持**低一致性**，不得换个数字就复用同一套模型和结构。具体要求：
+每道新题必须与已产出的所有题目保持**低一致性**：
 
-- **模型类型不重复**：主体几何体类型要有明显差异（如长方体、圆柱、六棱柱、圆台、圆盘、楔形、梯形台等交替使用），不得连续两题用同类型主体
-- **特征组合不重复**：核心特征类型要换（凸台/通孔/盲孔/凹槽/沉孔/偏心孔/切槽等交替），棱边处理也要换（圆角/倒角交替，或处理位置不同）
-- **尺寸量级有差异**：各题主体尺寸不要都在同一量级（如不要连续几题都是 50-80mm 见方），适当拉开大小差异
-- **结构组织不重复**：二级标题名称、段落顺序、信息合并方式每题不同，不得连续两题用同一套标题
-- **开篇和措辞不重复**：任务说明的句式、用词每题换，不得出现"这次建一个…""做一个…"连续重复
-- **Checklist 话术不重复**：各题 Checklist 的开头词、句式、表达角度全部不同，不得复用上一题的表述方式
-- **自检**：出完新题后，与最近 3 道题对比，确认模型类型、特征组合、标题结构、开篇句式、Checklist 话术至少有 4 项不同
+- **模型类型不重复**：主体几何体类型交替使用，不得连续两题用同类型主体
+- **特征组合不重复**：核心特征类型要换，棱边处理也要换
+- **尺寸量级有差异**：适当拉开大小差异
+- **结构组织不重复**：二级标题名称、段落顺序、信息合并方式每题不同
+- **开篇和措辞不重复**：任务说明的句式、用词每题换
+- **Checklist 话术不重复**：各题 Checklist 的开头词、句式、表达角度全部不同
+- **自检**：出完新题后，与最近 3 道题对比，确认至少有 4 项不同
 
 ### 3.4 验收核对清单（Checklist）硬性规则
 
-1. **严格对应题目内容，禁止加题外项**：所有验收条目必须 100% 来自题目明确给出的要求、参数、约束，不得新增题目未提及的判定标准。
-2. **按维度合并，可客观判定**：每条聚焦一个验收维度，可包含该维度下的多个数值/状态判据，整体可一次性判定通过/不通过，无模糊、主观表述。
-3. **条目总数控制在 5-6 条**：不得超过 6 条，也不得少于 5 条；通过按维度合并同类事实实现压缩，不得通过遗漏核心要求来凑数。
-4. **全覆盖无遗漏**：必须覆盖题目所有核心要求，包括：主体尺寸、核心特征尺寸、棱边处理参数、模型合规性（同一 Body、单一实体、Part Design 原生特征）、文件交付（命名、存储路径）、交付内容要求。
-5. **话术多变，禁止固定开头**：每条必须使用不同的句式结构和开头词，同一题目内各条开头词不得重复。禁止套路化表达连续出现。可交替使用"检查…是否…""确认…为…""…应满足…""…符合…""…达到…""…处于…""核实…""验证…"等多种表达。不同题目之间 Checklist 话术也要换，不复制上一题的句式。
-6. **格式**：用 ```plaintext 代码块包裹，条目编号清晰。
+Checklist 是简化版 Rubric，用于确认模型"做没做、做没做对、做没做好"。一条可同时校验若干紧密相关点，不要求为了原子化无限拆分；但必须覆盖所有关键验收点。
+
+#### 三层校验框架（构思时用）
+- **做没做（要求执行情况）**：模型是否执行了题目明确要求的任务，关键要求逐项覆盖，不遗漏核心任务
+- **做没做对（事实准确性）**：模型是否正确完成各项要求；需外部核验的事实补充可靠外验链接及结论；事实类验收点尽可能完整列出
+- **做没做好（产物质量）**：在完成任务的前提下，质量是否达到要求
+
+#### 硬规则
+1. **表述可判定**：每条均能由题目、产物或公开来源判定，避免"质量高""看起来专业"等空话。
+2. **非动作指令**：不使用"检查、查看、核对、浏览"等命令模型执行动作的词；改写成可判定的陈述或问题。
+3. **与评分说明一致**：通过/不通过的判定基于本条 Checklist，信息足以判定，不能只写几个字。
+4. **链接可用**：需外验的事实附真实、可打开、无需验证码或受限登录的链接。
+5. **完整/相关/相对独立**：关键点不漏、无题外项、条目之间尽量不重叠。
+6. **条目总数 5-6 条**：通过按维度合并实现，不得通过遗漏核心要求凑数。
+7. **话术多变**：每条开头词和句式不同，题目间也不重复。
+8. **格式**：用 ```plaintext 代码块包裹，条目编号清晰。
+
+#### 输出前自检维度（逐条对照题目，确认全覆盖）
+| 维度 | 是否必须有对应条目 |
+|------|-------------------|
+| 交付物文件存在、命名正确、路径正确 | 必须 |
+| 文件可正常打开、无损坏 | 必须 |
+| 题目中每个功能点/操作步骤 | 必须 |
+| 题目中每个量化参数（尺寸、数量、角度等） | 必须 |
+| 题目中的格式/命名/路径要求 | 必须 |
+| 题目中的"不要修改/不要删除"类约束 | 必须 |
+| 题目未提及的额外要求 | 禁止（无题外项） |
 
 ## 4. 风格与质量标准
 
 - 简洁、考点明确，避免非考点冗余内容
 - 建模难度控制为简单级，特征数量 2-3 个
 - 尺寸数值明确、定位关系清晰
-- Checklist 条目可客观判定，无主观模糊描述，总数 5-6 条
-- Checklist 每条话术不同，开头词不重复，无固定套路
+- Checklist 条目可客观判定，总数 5-6 条，话术多变
 - 语言自然，无AI套话和模板感
-- 每题结构和措辞有变化，不重复同一模板
-- 题目间低一致性：模型类型、特征组合、尺寸量级、标题结构、开篇句式、Checklist 话术均不重复
+- 题目间低一致性
+- 无歧义：多边形朝向、孔类型、棱边范围均明确
+- 建模与验收分离：独立验收脚本从磁盘回读验证
 
 ## 5. 例外与边界
 
@@ -88,6 +114,16 @@
 - 不得使用AI套话、排比堆砌、正确的废话
 - 不得每题套用完全相同的标题结构和措辞
 - Checklist 各条不得使用相同开头词或固定句式套路
+- Checklist 不得使用动作指令开头（检查/查看/核对/浏览等祈使动词）
+- Checklist 不得出现不可判定的空话（质量高/看起来专业等）
+- Checklist 不得遗漏"文件可正常打开、无损坏"这一必检项
+- Checklist 不得有题外项（题目未提及的额外要求）
 - 不得连续两题使用同类型主体几何体
 - 不得连续两题使用相同的核心特征类型
-- 不得换个数字就复用同一套模型结构和标题组织
+- 不得把脚本编写/无头调试与 GUI 初始化绑成硬门
+- 不得在目录不可写时卡死，必须有兜底
+- 不得使用"全新启动"而不说明残留进程处理
+- 多边形不得只给尺寸不标朝向
+- 盲孔不得不写平底/钻尖类型
+- 倒角/圆角不得不指定棱边范围
+- 建模脚本不得包含合格判定，验收必须独立脚本从磁盘回读

```
- 上一版主线完整归档：archive/main-v4-20260906-214524/（回滚命令：python cb.py rollback "E:\freecad题目创建" 4）

## v6 — 2026-09-06 21:46:08
- 内容回滚至归档：archive/main-v4-20260906-214524/
- 回滚说明：用户要求暂不提升，回滚到v4继续观察
- 回滚前主线已安全归档：archive/main-v5-20260906-214608-before-rollback/（版本号单调递增，历史不倒转）

## v7 — 2026-09-07 12:43:01
- 来源分支：task-style-v2（基于 v6）
- 提升说明：A-F六条题目条款+只交FCStd+7条写法改进+完整Checklist规范，用户确认效果满意
- 规则差异：

```diff
--- v6/PROMPT.md
+++ v7/PROMPT.md
@@ -1,7 +1,7 @@
-# 分支候选规则（branches/checklist-wording/PROMPT.md）
+# 分支候选规则（branches/task-style-v2/PROMPT.md）
 
-> 本文件是分支 checklist-wording 的候选规则，仅在本分支试跑期间生效。
-> 实验目的：Checklist 话术多变 + 题目间低一致性，避免固定模板和重复套路。
+> 本文件是分支 task-style-v2 的候选规则，仅在本分支试跑期间生效。
+> 实验目的：写法改进 + A-F六条题目条款 + 只交FCStd，快速出结果。
 
 ## 1. 任务定位
 
@@ -16,66 +16,119 @@
 
 ## 3. 长期提示词 / 工作规则
 
-### 3.1 题目内容要求（信息必达，表述自由）
+### 3.1 题目必含条款（信息必达，表述自由）
 
-以下信息点必须出现在题目中，但**组织方式、措辞、段落顺序均可灵活变化**，禁止每题套用同一模板：
+以下条款必须出现在题目中，但**组织方式、措辞、段落顺序均可灵活变化**，禁止每题套用同一模板：
 
-#### 必含信息点
-- **标题**：含"FreeCAD建模任务"和模型名称，格式不必每次相同
-- **任务说明**：用一两句话说清要建什么、建模后直接保存即可
-- **环境准备**：全新启动 FreeCAD 1.1.3 → Part Design 工作台 → 检查桌面 freecad 文件夹 → 输出"GUI 环境初始化完成"后才能写脚本
-- **脚本存放**：统一放 `E:\jiaobenmulu`，API 以 1.1.3 为准
-- **调试建议**：可用 freecadcmd 无头预验证，GUI 只跑最终脚本
+#### A. 环境一行前置信息
+必须写明：FreeCAD 1.1.3 安装路径（freecadcmd.exe 与 freecad.exe 同目录）、用户宏目录已配置为脚本存放目录、脚本存该目录、模型存桌面 freecad 文件夹。
+
+#### B. 几何口径无歧义定义
+- "对边距"指明为内切圆直径（=2×边心距），不是轮廓 Y 向总宽；多边形朝向写明（如"平边朝 +Y 即一条边水平且位于 +Y 侧"）
+- 可附参考量：边心距、外接圆半径、边长、顶点极角
+- 盲孔深度自顶面名义面向下量
+- 圆角/倒角对象精确指定（如"仅顶面与侧立面交线，孔口边、侧棱不倒"）
+
+#### C. GUI 执行通道
+- GUI 中先 Ctrl+N 新建文档使宏菜单可用，再经"宏 (M)→宏…→选中脚本→执行"运行一次
+- 禁止走 Python 控制台粘贴/键入（粘贴不稳定）
+- 脚本负责自建文档、存盘，并在末尾把 Body 的 ViewObject 设为可见、执行一次视图 FitAll，避免界面空白
+
+#### D. 草图约束策略
+- 约束方式不限，允许按精确计算坐标放置几何后用 Block 约束整体锁位
+- 要求全约束、单闭合线框
+- 不强制"构造圆 + 等长"类参数化约束
+
+#### E. 验收口径与理论值（定义正确性，不要求提交验收产物）
+题目必须给出可验证的验收口径和理论值，至少覆盖：
+- 单一 Body / 单一实体
+- 特征 TypeId 白名单（Sketcher::SketchObject、PartDesign::Pad/Pocket/Fillet 等），禁 Part 造体与布尔
+- 关键水平截面形状与面积（用 Part.Face(wire).Area）
+- 几何参数理论值（边长、内角、边心距、孔壁半径、孔深范围等）
+- 特征面数量与参数（如圆柱面数量、半径、轴线位置、高度）
+- 理论体积（带容差，如 1%）
+具体数值因题而异，但每题必须给出。
+
+#### F. 调试纪律
+- 建模脚本要求零求解器告警（告警即换约束方案，同一方案最多试两次）
+- 若使用验收脚本，异常与结论必须同时写日志文件落盘，不得只依赖控制台输出
+
+#### 其他必含信息
+- **标题**：含"FreeCAD建模任务"和模型名称
+- **任务说明**：一两句话说清要建什么
+- **环境准备**：先结束所有残留 FreeCAD 进程，再全新启动 → Part Design 工作台 → 检查桌面 freecad 文件夹
+- **脚本与调试解耦**：脚本编写和 freecadcmd 无头调试可先行；GUI 仅执行一次最终脚本
+- **脚本存放与兜底**：脚本统一放宏目录；写入被拒排查一次仍不行则暂存桌面 freecad 文件夹并注明
 - **模型约束**：Part Design 原生特征、同一 Body、单一实体、禁止 Part 模块直生几何体、禁止布尔拼接
-- **尺寸参数**：XY 平面基准、+Z 拉伸、Z=0 底面；主体尺寸 + 核心特征尺寸 + 棱边处理（倒角/圆角）；特征 2-3 个；数值明确、定位清晰
+- **尺寸参数**：XY 平面基准、+Z 拉伸、Z=0 底面；主体 + 核心特征 + 棱边处理；特征 2-3 个；数值明确
+- **文件覆盖**：同名 FCStd 直接覆盖，不另存版本号
 - **操作准则**：单卡点最多两种思路，第一种两次失败即切换；报错先找根因再整体重跑
-- **交付标准**：英文文件名.FCStd → 桌面 freecad 文件夹 → 交付绝对路径 + 模型树特征顺序
+- **只建模不验收**：题目定义验收口径但不要求提交验收脚本/报告；建模脚本跑完存盘即完成
+- **交付标准**：仅提交 FCStd 文件，存桌面 freecad 文件夹；不提交脚本、验收报告等额外产物
 
 #### 表述自由度
-- 不必每次都用"执行要求""尺寸参数""操作准则""交付标准"这几个完全相同的二级标题
-- 可以把环境准备和脚本规范合并成一段，也可以把调试建议融入模型约束
-- 开篇说明可以换说法，不必每次都是同一句
-- 核心是信息到位、读起来像人写的，而不是填表
+- 不必每次都用固定的二级标题，可自由组织
+- 可以把相关信息合并成一段，也可以拆开
+- 开篇说明可以换说法
+- 核心是信息到位、读起来像人写的
 
 ### 3.2 语言风格（去AI味）
 
-- **说人话**：用自然口语化的书面表达，避免"综上所述""值得注意的是""需要指出的是"这类AI套话
-- **不堆排比**：不要每条都用相同句式开头（如连续的"必须……""应当……""确保……"）
-- **不写正确的废话**：删掉"请仔细阅读以下要求""按照步骤逐步完成"这类无信息量的句子
-- **长短句交错**：避免全是结构工整的长句，适当用短句、断句
-- **每题换说法**：同一信息点在不同题目中用不同措辞表达，不复制粘贴
+- **说人话**：自然口语化书面表达，避免AI套话
+- **不堆排比**：不要每条都用相同句式开头
+- **不写正确的废话**：删掉无信息量的句子
+- **长短句交错**：适当用短句、断句
+- **每题换说法**：同一信息点在不同题目中用不同措辞
 
 ### 3.3 题目间低一致性（硬性要求）
 
-每道新题必须与已产出的所有题目保持**低一致性**，不得换个数字就复用同一套模型和结构。具体要求：
-
-- **模型类型不重复**：主体几何体类型要有明显差异（如长方体、圆柱、六棱柱、圆台、圆盘、楔形、梯形台等交替使用），不得连续两题用同类型主体
-- **特征组合不重复**：核心特征类型要换（凸台/通孔/盲孔/凹槽/沉孔/偏心孔/切槽等交替），棱边处理也要换（圆角/倒角交替，或处理位置不同）
-- **尺寸量级有差异**：各题主体尺寸不要都在同一量级（如不要连续几题都是 50-80mm 见方），适当拉开大小差异
-- **结构组织不重复**：二级标题名称、段落顺序、信息合并方式每题不同，不得连续两题用同一套标题
-- **开篇和措辞不重复**：任务说明的句式、用词每题换，不得出现"这次建一个…""做一个…"连续重复
-- **Checklist 话术不重复**：各题 Checklist 的开头词、句式、表达角度全部不同，不得复用上一题的表述方式
-- **自检**：出完新题后，与最近 3 道题对比，确认模型类型、特征组合、标题结构、开篇句式、Checklist 话术至少有 4 项不同
+- **模型类型不重复**：不得连续两题用同类型主体
+- **特征组合不重复**：核心特征类型和棱边处理要换
+- **尺寸量级有差异**：适当拉开大小差异
+- **结构组织不重复**：二级标题、段落顺序、信息合并方式每题不同
+- **开篇和措辞不重复**：句式、用词每题换
+- **Checklist 话术不重复**：开头词、句式、表达角度全部不同
+- **自检**：出完与最近 3 题对比，至少 4 项不同
 
 ### 3.4 验收核对清单（Checklist）硬性规则
 
-1. **严格对应题目内容，禁止加题外项**：所有验收条目必须 100% 来自题目明确给出的要求、参数、约束，不得新增题目未提及的判定标准。
-2. **按维度合并，可客观判定**：每条聚焦一个验收维度，可包含该维度下的多个数值/状态判据，整体可一次性判定通过/不通过，无模糊、主观表述。
-3. **条目总数控制在 5-6 条**：不得超过 6 条，也不得少于 5 条；通过按维度合并同类事实实现压缩，不得通过遗漏核心要求来凑数。
-4. **全覆盖无遗漏**：必须覆盖题目所有核心要求，包括：主体尺寸、核心特征尺寸、棱边处理参数、模型合规性（同一 Body、单一实体、Part Design 原生特征）、文件交付（命名、存储路径）、交付内容要求。
-5. **话术多变，禁止固定开头**：每条必须使用不同的句式结构和开头词，同一题目内各条开头词不得重复。禁止套路化表达连续出现。可交替使用"检查…是否…""确认…为…""…应满足…""…符合…""…达到…""…处于…""核实…""验证…"等多种表达。不同题目之间 Checklist 话术也要换，不复制上一题的句式。
-6. **格式**：用 ```plaintext 代码块包裹，条目编号清晰。
+Checklist 是简化版 Rubric，用于确认模型"做没做、做没做对、做没做好"。一条可同时校验若干紧密相关点，但必须覆盖所有关键验收点。
+
+#### 三层校验框架
+- **做没做**：模型是否执行了题目明确要求的任务
+- **做没做对**：模型是否正确完成各项要求，事实类验收点尽可能完整列出
+- **做没做好**：质量是否达到要求
+
+#### 硬规则
+1. **表述可判定**：每条均能由题目、产物判定，避免空话
+2. **非动作指令**：不使用"检查、查看、核对、浏览"等词；改写成可判定的陈述或问题
+3. **与评分说明一致**：信息足以判定，不能只写几个字
+4. **完整/相关/相对独立**：关键点不漏、无题外项、条目间尽量不重叠
+5. **条目总数 5-6 条**：按维度合并实现，不得遗漏核心要求
+6. **话术多变**：每条开头词和句式不同，题目间也不重复
+7. **格式**：用 ```plaintext 代码块包裹，条目编号清晰
+
+#### 输出前自检维度
+| 维度 | 是否必须有对应条目 |
+|------|-------------------|
+| 交付物文件存在、命名正确、路径正确 | 必须 |
+| 文件可正常打开、无损坏 | 必须 |
+| 题目中每个功能点/操作步骤 | 必须 |
+| 题目中每个量化参数 | 必须 |
+| 题目中的格式/命名/路径要求 | 必须 |
+| 题目中的"不要…"类约束 | 必须 |
+| 题目未提及的额外要求 | 禁止 |
 
 ## 4. 风格与质量标准
 
 - 简洁、考点明确，避免非考点冗余内容
-- 建模难度控制为简单级，特征数量 2-3 个
-- 尺寸数值明确、定位关系清晰
-- Checklist 条目可客观判定，无主观模糊描述，总数 5-6 条
-- Checklist 每条话术不同，开头词不重复，无固定套路
+- 建模难度简单级，特征 2-3 个
+- 尺寸数值明确、几何口径无歧义
+- Checklist 可判定，总数 5-6 条，话术多变
 - 语言自然，无AI套话和模板感
-- 每题结构和措辞有变化，不重复同一模板
-- 题目间低一致性：模型类型、特征组合、尺寸量级、标题结构、开篇句式、Checklist 话术均不重复
+- 题目间低一致性
+- 题目含 A-F 全部条款，验收口径与理论值给死
+- 只建模不验收，交付仅 FCStd 文件，快速出结果
 
 ## 5. 例外与边界
 
@@ -83,11 +136,22 @@
 - 不得新增题目未要求的验收标准
 - 不得出现主观模糊描述
 - 不得遗漏题目明确的交付与约束要求
-- 不得使用复杂结构模型，控制建模难度为简单级
+- 不得使用复杂结构模型
 - Checklist 不得超过 6 条或少于 5 条
 - 不得使用AI套话、排比堆砌、正确的废话
 - 不得每题套用完全相同的标题结构和措辞
-- Checklist 各条不得使用相同开头词或固定句式套路
+- Checklist 各条不得使用相同开头词或固定句式
+- Checklist 不得使用动作指令开头
+- Checklist 不得遗漏"文件可正常打开、无损坏"
+- Checklist 不得有题外项
 - 不得连续两题使用同类型主体几何体
 - 不得连续两题使用相同的核心特征类型
-- 不得换个数字就复用同一套模型结构和标题组织
+- 不得把脚本编写/无头调试与 GUI 初始化绑成硬门
+- 不得在目录不可写时卡死
+- 不得使用"全新启动"而不说明残留进程处理
+- 多边形不得不标朝向
+- 盲孔不得不写平底/钻尖类型
+- 倒角/圆角不得不指定棱边范围
+- 不得要求提交验收脚本或验收报告
+- 不得走 Python 控制台粘贴执行脚本
+- 建模脚本不得有求解器告警

```
- 上一版主线完整归档：archive/main-v6-20260907-124301/（回滚命令：python cb.py rollback "E:\freecad题目创建" 6）
