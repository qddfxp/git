# FreeCAD建模任务：带中心凸台的圆角长方体

请使用 FreeCAD 完成带中心凸台的圆角长方体的参数化建模，模型生成后直接保存文件即可，无需额外开展尺寸核对、特征校验类操作，精简执行流程。

## 执行要求

1. **前置准备**：全新启动 FreeCAD 1.1.3 客户端，处理弹窗至主界面稳定运行，切换至 Part Design 工作台；检查桌面 freecad 存储文件夹，不存在则新建。准备就绪后输出 "GUI 环境初始化完成"，在此之前不得编写任何建模脚本或创建代码文件。

2. **脚本规范**：建模所用 Python 脚本统一存放至 `E:\jiaobenmulu` 目录，禁止写入软件安装路径或系统目录；API 属性调用以 1.1.3 版本为准。

3. **效率提示**：支持无头预验证模式，可先通过 freecadcmd 完成脚本逻辑调试，GUI 仅执行一次最终脚本即可，无需界面反复调试。

4. **模型约束**：最终实体必须为 Part Design 工作台下的原生参数化特征，全部特征归属同一 Body 并自动融合为单一实体；禁止使用 Part 模块直接生成几何体，也不允许通过布尔运算拼接模型。

## 尺寸参数

- **建模基准**：XY 平面为草图基准，+Z 为拉伸方向，Z=0 平面为模型底面。
- **主体尺寸**：长方体，长 80 mm（X 方向）、宽 60 mm（Y 方向）、高 20 mm（Z 方向），草图矩形中心与原点重合。
- **核心特征**：顶面中心同轴圆柱凸台，直径 30 mm，高度 15 mm，凸台底面与主体顶面重合，凸台轴线与主体中心轴线（过原点的 Z 轴）同轴。
- **棱边处理**：主体底面四条棱边倒圆角，圆角半径 R5 mm。

## 操作准则

单个卡点最多构思两种解决思路，第一种方案尝试两次仍无法推进则立刻切换第二种，不重复已验证失败的路径；脚本报错先定位根本原因，修正后整体重跑，不逐行补丁式调试。

## 交付标准

1. **文件命名**：`block_with_boss.FCStd`
2. **存储位置**：桌面 freecad 文件夹
3. **交付内容**：文件完整绝对路径 + 模型树特征排列顺序

---

## 验收核对清单（Checklist）

```plaintext
1. 模型文件名为 block_with_boss.FCStd
2. 模型文件存储于桌面 freecad 文件夹
3. 交付内容包含文件完整绝对路径
4. 交付内容包含模型树特征排列顺序
5. 全部特征归属同一 Body
6. 最终模型为单一实体
7. 所有特征均为 Part Design 工作台原生参数化特征
8. 未使用 Part 模块直接生成几何体
9. 未通过布尔运算拼接模型
10. 主体长方体 X 方向长度为 80 mm
11. 主体长方体 Y 方向宽度为 60 mm
12. 主体长方体 Z 方向高度为 20 mm
13. 主体草图基准为 XY 平面
14. 主体拉伸方向为 +Z
15. 主体底面位于 Z=0 平面
16. 主体草图矩形中心与原点重合
17. 凸台直径为 30 mm
18. 凸台高度为 15 mm
19. 凸台底面与主体顶面重合
20. 凸台轴线与主体中心轴线（过原点的 Z 轴）同轴
21. 主体底面四条棱边倒圆角
22. 圆角半径为 5 mm
```
